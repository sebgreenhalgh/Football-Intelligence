# Football Intelligence current state

## Active strand

```text
Football Observation Reasoner / G7E temporal burst review
VISUAL_ONLY_NOT_METRIC
TRAIN_DEVELOPMENT_ONLY
BURST_LOCAL_CONTINUITY_ONLY
NO_PERMANENT_IDENTITY
production_ready=false
```

The latest completed architecture stage is R6, the server-authoritative action reducer. R6.1 added release-gated low-light display modes. R6.2 is the current release-gated precision-navigation revision: its source may be committed before acceptance, but real review remains locked until an exact-final-byte gate validates cursor-anchored zoom, all pan modes, independent panorama/detail state, normalized Lock view, fullscreen preservation, source-coordinate round trips, the commit, reviewer package, browser bundles, CSS, reducer, server module, and contracts.

## Frozen temporal corpus

- 120 bursts across six `TRAIN_DEVELOPMENT` matches;
- nine exact frame references per burst (1,080 total);
- six independently completable tranches;
- burst-local `SUBJECT_A`, `SUBJECT_B`, and `SUBJECT_C` only;
- team classification excluded from this wave;
- frozen frame, candidate, and tranche provenance must not change.

## Real human state

Real decisions live outside Git in the authorised decision root. They are immutable once acknowledged. The active draft is server-backed and may be resumed only through the release-gated reviewer. Counts and the active question are discovered from local state at release time; documentation never substitutes for that state.

## Reviewer architecture

- every browser edit is a canonical versioned action;
- action ID and idempotency key are the same canonical UUID;
- accepted actions persist draft, action receipt, and ledger through a recoverable transaction journal;
- summary is server-authorised only when applicable question lifecycles resolve;
- final save persists an immutable event before acknowledgement;
- tranche and global receipts bind exact latest acknowledged event sets;
- Original, Enhanced, and Auto are display-only modes with unchanged image dimensions and source coordinates.

## Development defaults

The approved pitch gate applies only to eligible `TRAIN_DEVELOPMENT` runs with a hash-valid human-confirmed, match-stable pitch polygon and complete external audit root. Explicit `DISABLED` takes precedence. Validation, holdout, production, and historical reproduction remain excluded. Nested suppression is not active.

## Boundaries

No production selector, tracking identity, temporal model, tactical metric, or physical metric is approved. Validation and sealed holdout are not part of the active reviewer stage. `production_ready=false` remains mandatory.
