# G7D-B1 static foldwise runtime v1

Decision: `PASS_G7D_B1_PROPOSAL_CLOSURE_AND_FOLDWISE_RUNTIME_READY_FOR_BASELINE_RERUN`

Contract ID: `G7D_B1_STATIC_FOLDWISE_RUNTIME_V1`

Parent: `G7D_B0_FOLDWISE_DIAGNOSTIC_UNSEEN_MATCH_RUNTIME_V1`

The exact unseen-input G6E proposal dependency chain is closed and hash-bound. The runtime uses the retained detector and fixed G6E S0/S3 preprocessing, NMS, coordinate replay, proposal-node construction, and `IOU_CONNECTED_COMPONENT_055` consolidation behavior without substitution.

Each candidate receives five independent N3 outputs in deterministic fold order `0, 1, 2, 3, 4`. Each fold uses its matching retained checkpoint, exactly reproduced outer-training normalizer, and exact retained N4 per-head temperatures. No fold is primary and no cross-fold selection, reduction, voting, averaging, suppression, or acceptance is defined.

P2 and P3 remain disabled pending a separately authorized exact state rebuild. H0, H1, H2, H3, the failed hierarchical selector, identity, tracking, and production observation acceptance remain excluded.

The two authorized development smoke frames establish executable and provenance closure only. They are not ground truth and make no transfer-performance or production-readiness claim. `production_ready=false`.

The next permitted stage is `G7D_B2_FROZEN_128058_BASELINE_RERUN` under this exact runtime. No threshold tuning or runtime change is authorized by this decision.
